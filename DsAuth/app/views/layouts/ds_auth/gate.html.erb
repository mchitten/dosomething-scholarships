<!DOCTYPE html>
<html>
<head>
  <% if Rails.env.production? %>
    <%= render 'partials/analytics' %>
  <% end %>

  <title><%= get_campaign ? get_campaign.title : "DoSomething Create and Share Campaigns" %></title>
  <%= campaign_stylesheet_link_tag('gate', get_campaign) %>
  <%= campaign_javascript_include_tag('application', get_campaign) %>
  <%= campaign_javascript_include_tag('gate', get_campaign) %>

  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
  <meta property="og:title" content="Pics for Pets" />
  <meta property="og:description" content="Over half of the 5 to 7 million pets that enter shelters are put to sleep.  Help save their lives just by hitting 'share'." />
  <meta property="og:image" content="http://pics.dosomething.org/images/logo-pics.png" />
  <%= csrf_meta_tags %>
</head>
<body class="wrapper">

<%# @TODO - BREAK GATE COMPONENTS INTO PARTIALS %>
<% if Rails.env.development? %>
  <%= render 'partials/debug' %>
  <%= link_to 'debug', '#', { :id => 'debug' } %>
<% end %>

<%= yield %>

</body>
</html>
